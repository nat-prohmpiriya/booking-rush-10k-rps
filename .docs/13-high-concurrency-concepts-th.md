# High Concurrency & System Design (Booking Rush 10k RPS)

## 📌 5 Main Pillars (หัวใจหลักของระบบ)

### 1. Hybrid Architecture (Fast-Path + Saga)
**Concept:** แยกการทำงานออกเป็น 2 โหมดคือ "เร็ว (Synchronous)" และ "ช้า (Asynchronous)"
*   **Fast Path (จอง):** ใช้ Redis ยิงตรง ไม่รอ DB เพื่อให้ได้ Latency ต่ำที่สุด (`< 50ms`) รับมือแรงกระแทก 10,000 คนพร้อมกันไหว
*   **Slow Path (ยืนยัน):** ใช้ Kafka + Saga Pattern จัดการเรื่องจ่ายเงินและยืนยันตั๋วทีหลัง เพื่อความชัวร์ (Consistency)
*   **Keyword:** `Synchronous vs Asynchronous`, `Eventual Consistency`

### 2. Redis Atomic Operations (Lua Script)
**Concept:** กุญแจสำคัญที่ทำให้ระบบ "ไม่ขายตั๋วเกิน (Zero Overselling)"
*   **How:** ใช้ภาษา Lua เขียน Logic ตัดสต็อก ยัดใส่ Redis ให้ทำงานรวดเดียวจบ (Atomic) เหมือนเป็นคำสั่งเดียว
*   **Key Benefit:** เร็วกว่า DB Transaction ทั่วไป 10-100 เท่า และแก้ปัญหา Race Condition ได้ 100%
*   **Keyword:** `Atomicity`, `Race Condition`, `Single-threaded execution`

### 3. Virtual Queue (Waiting Room)
**Concept:** "เขื่อนกั้นน้ำ" ป้องกันเซิร์ฟเวอร์ตาย
*   **How:** เมื่อคนเข้ามาเกิน 10,000 คน ระบบจะแจกบัตรคิว (Queue Token) ให้รอ แล้วค่อยๆ ปล่อยคนเข้าไประบบจองทีละนิด (e.g. 500 คน/วินาที)
*   **Key Benefit:** เปลี่ยน Traffic แบบ Spike (พุ่งสูง) ให้เป็น Smooth (ราบเรียบ) เซิร์ฟเวอร์ไม่น็อก
*   **Keyword:** `Thundering Herd Problem`, `Backpressure`, `Rate Limiting`

### 4. Idempotency (กันเบิ้ล)
**Concept:** "กดกี่ครั้ง ผลลัพธ์ต้องเหมือนเดิม"
*   **How:** ใช้ key พิเศษ (`Idempotency-Key`) แนบมากับ Request ถ้า Server เห็น Key ซ้ำ จะไม่ทำรายการใหม่ แต่ส่งผลลัพธ์เก่ากลับไป
*   **Key Benefit:** กัน User รัวปุ่ม, กัน Network Retry ตัดเงินซ้ำ
*   **Keyword:** `Idempotency Key`, `At-least-once delivery`, `Deduplication`

### 5. Distributed Tracing (Observability)
**Concept:** "เห็นทุกอย่าง" ในระบบที่ซับซ้อน
*   **How:** ใช้ OpenTelemetry แปะ Trace ID ไปกับทุก Request ทำให้รู้ว่า Request นี้วิ่งผ่าน Service ไหนบ้าง ช้าที่ตรงไหน (DB, Redis, หรือ Kafka)
*   **Tool:** Jaeger, Tempo, Grafana
*   **Keyword:** `Observability`, `Telemetry`, `Spans & Traces`, `Bottleneck Analysis`

---

## 🛠 6 Advanced Concepts (ของแถมสำหรับระดับ Senior)

### 1. Token Bucket Algorithm
*   **คืออะไร:** อัลกอริทึมที่ใช้ทำ **Rate Limiter**
*   **หลักการ:** เหมือนเติมเหรียญใส่ถังด้วยความเร็วคงที่ ถ้าเหรียญหมดถัง User ต้องรอ (Rate Limited) เราใช้ Redis คุมถังนี้
*   **Keyword:** `Algorithm`, `Request throtlling`

### 2. Database Connection Pooling
*   **คืออะไร:** การเปิดท่อเชื่อมต่อ Database ค้างไว้ (Pool) ให้หยิบใช้ได้เลย ไม่ต้องต่อใหม่ทุกครั้ง
*   **Tuning:** เราตั้ง `MaxConns` ไว้ต่ำ (20) เพราะ Traffic ส่วนใหญ่ถูกกันด้วย Queue และ Redis แล้ว (Database ไม่ได้รับ load ตรงๆ)
*   **Keyword:** `Resource Management`, `Multiplexing`

### 3. Horizontal Scaling vs Vertical Scaling
*   **คืออะไร:** วิธีขยายระบบเมื่อคนใช้เยอะขึ้น
*   **Vertical (Scale Up):** เพิ่ม RAM/CPU ให้เครื่องเดียวแรงขึ้น (มีขีดจำกัด แพง)
*   **Horizontal (Scale Out):** เพิ่มจำนวนเครื่อง/Pods ให้ช่วยกันรับงาน (Project นี้ใช้แบบนี้ โดยรันบน K3s/Docker)
*   **Keyword:** `Distributed System`, `Stateless Service`

### 4. C10k / C10M Problem
*   **คืออะไร:** ปัญหาคลาสสิกของ Server ว่าจะรับ Connection หมื่น/ล้านพร้อมกันยังไง
*   **Solution:** ภาษา Go ใช้ **Goroutines** (Green Threads) ที่เบามาก (2KB) ทำให้รับ Connection ได้เยอะกว่าภาษาอื่นที่ใช้ OS Thread (1MB)
*   **Keyword:** `Concurrency Model`, `Non-blocking I/O`

### 5. Load Balancing Strategies
*   **คืออะไร:** เทคนิคการกระจายงานให้ Server หลายๆ เครื่องเท่าๆ กัน
*   **How:** API Gateway / Nginx จะเลือกส่ง Request ไปเครื่องไหน? (Round Robin = วนไป, Least Connection = ส่งให้เครื่องงานน้อยสุด)
*   **Implementation ในโปรเจกต์นี้:**
    *   **Layer 1 - Traefik Ingress Controller:** รับ traffic จากภายนอก กระจายไปยัง API Gateway pods (ใช้ Round Robin)
    *   **Layer 2 - Kubernetes Service:** แต่ละ service (เช่น `api-gateway`, `booking-service`) มี `ClusterIP` ที่ทำ load balancing ด้วย **iptables/IPVS** (Round Robin)
    *   **Layer 3 - Custom Reverse Proxy:** `backend-api-gateway/internal/proxy/proxy.go` เป็น application-level routing ส่ง request ไปยัง backend services ตาม path
*   **Replicas:** API Gateway ตั้งไว้ 2 replicas สำหรับ High Availability
*   **Connection Pooling:** Proxy ใช้ `MaxIdleConnsPerHost: 100` เพื่อ reuse connections
*   **Keyword:** `Round Robin`, `Least Connections`, `Reverse Proxy`, `Ingress Controller`

### 6. Transactional Outbox Pattern
*   **คืออะไร:** วิธีส่ง Kafka Event ให้ชัวร์ 100% แม้ระบบล่ม
*   **How:** บันทึก Event ลง Database ก่อน (ใน Transaction เดียวกับ Booking) แล้วค่อยมี Worker มาอ่านไปส่ง Kafka
*   **Key Benefit:** แก้ปัญหา "บันทึก DB สำเร็จ แต่ส่ง Kafka ไม่ไป" (Consistency)
*   **Keyword:** `Dual Write Problem`, `Reliability`

---

## 📚 Vocabulary List (คำศัพท์ต้องรู้)

| คำศัพท์ | คำอ่าน | ความหมายสั้นๆ |
|:---|:---|:---|
| **High Concurrency** | ไฮ-คอน-เคอร์-เรน-ซี | ระบบที่รองรับคนใช้งานพร้อมกันจำนวนมาก |
| **Throughput (RPS)** | ทรู-พุต | ปริมาณงานที่ระบบทำได้ต่อวินาที (Requests Per Second) |
| **Latency** | เล-เทน-ซี | ความหน่วง (เวลาที่ใช้ตอบกลับ) ยิ่งต่ำยิ่งดี |
| **Race Condition** | เรซ-คอน-ดิ-ชัน | บั๊กที่เกิดจากหลาย thread แย่งกันแก้ไขข้อมูลเดียวกัน |
| **Atomic** | อะ-ตอม-มิก | การทำงานแบบ "เสร็จทั้งหมด" หรือ "ไม่ทำเลย" (ไม่มีครึ่งๆ กลางๆ) |
| **Backpressure** | แบ็ค-เพรส-เชอร์ | แรงดันย้อนกลับ (เมื่อระบบหลังบ้านทำไม่ทัน ต้องบอกหน้าบ้านให้หยุดส่ง) |
| **Idempotency** | ไอ-เด็ม-โพ-เทน-ซี | การทำซ้ำได้ผลลัพธ์เท่าเดิม |
| **Eventual Consistency** | อี-เวน-ชวล-คอน-ซิส-เทน-ซี | (ความสม่ำเสมอภายหลัง) ข้อมูลจะไม่ตรงกันทันที แต่สุดท้ายจะตรงกันเอง (ยอมรับ Delay ได้) |
| **Scalability** | สเกล-ลา-บิ-ลิ-ตี้ | ความสามารถในการขยายระบบ (เพิ่มเครื่อง) เพื่อรับ load เพิ่ม |
| **Stateless** | สเตท-เลส | บริการที่ไม่จำข้อมูล User (Scale ง่ายกว่า Stateful) |
| **TTL** | ที-ที-แอล | Time To Live - อายุของข้อมูล หมดเวลาก็หายไปเอง |
| **Dual-TTL** | ดูอัล-ที-ที-แอล | กลยุทธ์ใช้ TTL 2 ค่าสำหรับ Processing และ Completed |

---

## ⏱️ TTL & Dual-TTL Strategy (กลยุทธ์กำหนดอายุข้อมูล)

### TTL คืออะไร?
**TTL (Time To Live)** คือ "อายุ" ของข้อมูลที่เก็บใน Redis/Cache พอหมดเวลา ข้อมูลจะถูกลบอัตโนมัติ

**ทำไมต้องใช้ TTL:**
*   **ป้องกัน Memory เต็ม:** ข้อมูลเก่าจะหายไปเอง ไม่ต้องลบ manual
*   **Data Freshness:** ข้อมูลจะ fresh เสมอ ไม่มี stale data ค้างอยู่
*   **Automatic Cleanup:** ไม่ต้องเขียน cron job มาลบข้อมูลเก่า

### TTL ในโปรเจกต์นี้
| ข้อมูล | TTL | เหตุผล |
|:---|:---|:---|
| **Reservation (การจอง)** | 10 นาที | ให้เวลาจ่ายเงิน ถ้าไม่จ่ายจะคืนที่นั่งอัตโนมัติ |
| **Queue Token (บัตรคิว)** | 30 นาที | ให้เวลารอคิว ถ้านานเกินไปต้องต่อคิวใหม่ |
| **Queue Pass (ใบผ่านเข้า)** | 5 นาที | ได้สิทธิ์แล้วต้องรีบใช้ |
| **Rate Limit Entry** | 1 นาที | นับ request ต่อนาที |

---

### Dual-TTL Strategy (กลยุทธ์ TTL คู่)

**ปัญหา:** ระบบ Idempotency ต้องจำว่า request นี้เคยทำแล้วหรือยัง
*   ถ้าตั้ง TTL สั้นเกินไป → request อาจทำซ้ำ (เพราะลืมไปแล้ว)
*   ถ้าตั้ง TTL ยาวเกินไป → ถ้า request ค้าง (processing) จะ block นานเกินไป

**Solution: ใช้ TTL 2 ค่า**

```
┌─────────────────────────────────────────────────────────────┐
│                    Dual-TTL Strategy                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   Request เข้ามา                                            │
│        │                                                    │
│        ▼                                                    │
│   ┌─────────────┐                                           │
│   │  PROCESSING │ ◄── TTL สั้น (60 วินาที)                   │
│   │   Status    │     ถ้าค้างหรือ crash จะหายไปเร็ว           │
│   └──────┬──────┘     ให้ request ใหม่เข้ามาได้              │
│          │                                                  │
│          ▼ (เสร็จแล้ว)                                       │
│   ┌─────────────┐                                           │
│   │  COMPLETED  │ ◄── TTL ยาว (24 ชั่วโมง)                   │
│   │   Status    │     จำไว้นาน ป้องกันทำซ้ำ                   │
│   │  + Response │     ถ้า request มาซ้ำ → ส่ง response เดิม  │
│   └─────────────┘                                           │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**Implementation:** `pkg/middleware/idempotency.go`
```go
ProcessingTTL: 60 * time.Second,  // กำลังทำอยู่ → หายใน 60 วิ ถ้าค้าง
TTL:           24 * time.Hour,    // เสร็จแล้ว → จำไว้ 24 ชม.
```

**ประโยชน์:**
1.  **Self-healing:** ถ้า request ค้าง (server crash ระหว่าง processing) จะ unlock ภายใน 60 วินาที
2.  **Duplicate protection:** request ที่เสร็จแล้วจะถูกจำไว้ 24 ชั่วโมง
3.  **Memory efficient:** ไม่เก็บ processing state นานเกินจำเป็น

**Keyword:** `TTL`, `Expiration`, `Self-healing`, `Idempotency`
